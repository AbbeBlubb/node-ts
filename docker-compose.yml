version: "3.8"
services:
  server:
    build:
      context: .
      dockerfile: Dockerfile.dev
      # > Successfully tagged compose-volumes_client:latest
    restart: on-failure
    ports:
      - "3000:6000"
    volumes: 
      - .:/app
      - /app/node_modules
